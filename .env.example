APP_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Neon Postgres is the default database. Copy the connection string from the
# Neon Console and keep its password out of source control.
DATABASE_PROVIDER=postgres
DATABASE_URL=postgresql://neondb_owner:REPLACE_ME@YOUR_NEON_ENDPOINT/neondb?sslmode=require&channel_binding=require
DATABASE_POOL_MAX=10
DATABASE_CONNECT_TIMEOUT_SECONDS=10
DATABASE_IDLE_TIMEOUT_SECONDS=20

# Offline/local fallback when Neon is not available.
# DATABASE_PROVIDER=sqlite
# DATABASE_URL=file:./data/mathios.db

STORAGE_PROVIDER=local
STORAGE_ROOT=./data/storage
STORAGE_MAX_UPLOAD_BYTES=26214400
STORAGE_SIGNED_URL_TTL_SECONDS=900

# S3-compatible storage (required for APP_ENV=hosted-production).
# S3_ENDPOINT=https://s3.example.com
# S3_REGION=us-east-1
# S3_BUCKET=mathios-production
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_SESSION_TOKEN=
S3_FORCE_PATH_STYLE=false

# Neon Auth provides email/password and Google authentication through the
# checked-in /api/auth route and auth UI. Generate a fresh 32+ character secret.
AUTH_MODE=neon-auth
NEON_AUTH_BASE_URL=https://ep-super-firefly-awxtpipg.neonauth.c-12.us-east-1.aws.neon.tech/neondb/auth
NEON_AUTH_COOKIE_SECRET=replace-with-a-random-secret-at-least-32-characters

# Offline/local fallback.
# AUTH_MODE=local-profile

# Legacy hosted auth verifies Authorization: Bearer JWT tokens with HS256 or RS256.
# HOSTED_AUTH_ISSUER=https://issuer.example.com/
# HOSTED_AUTH_AUDIENCE=mathios
# HOSTED_AUTH_SHARED_SECRET=
# HOSTED_AUTH_PUBLIC_KEY=

SEARCH_PROVIDER=local
AI_PROVIDER=disabled
AI_LOCAL_BASE_URL=http://127.0.0.1:11434
AI_LOCAL_MODEL=llama3.2
AI_REMOTE_BASE_URL=https://api.openai.com/v1
AI_REMOTE_MODEL=gpt-4o-mini
# Optional server-only fallback key; the AI settings screen stores keys encrypted instead.
# AI_REMOTE_API_KEY=
AI_REQUEST_TIMEOUT_MS=15000

# Development fallback only. Replace with a random 32+ character secret outside local development.
SESSION_SECRET=change-me-in-development-only-change-me
SESSION_ROTATION_INTERVAL_SECONDS=86400
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_SECONDS=60
RATE_LIMIT_MAX_REQUESTS=300
AUTH_RATE_LIMIT_WINDOW_SECONDS=900
AUTH_RATE_LIMIT_MAX_ATTEMPTS=10
CSRF_PROTECTION_ENABLED=true
TRUST_PROXY=false
# ERROR_TRACKING_DSN=https://errors.example.com/api/events
# METRICS_TOKEN=
LOG_LEVEL=info
